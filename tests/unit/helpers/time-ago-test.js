import {
  timeAgo
} from 'rails-tutorial-ember/helpers/time-ago';

module('TimeAgoHelper');

// Replace this with your real tests.
test('it works', function() {
  var result = timeAgo(42);
  ok(result);
});
